version: '3.3'
services:
  nginx:
    container_name: annual_reports
    build:
      context: ./
      dockerfile: Dockerfile-local
    volumes:
      - ./:/usr/share/nginx/html/
    ports:
      - '8080:80'
